'use strict';

var GetIntrinsic = require('es-abstract/GetIntrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var DefineOwnProperty = require('es-abstract/helpers/DefineOwnProperty');

var FromPropertyDescriptor = require('es-abstract/2016/FromPropertyDescriptor');
var IsDataDescriptor = require('es-abstract/2016/IsDataDescriptor');
var IsPropertyKey = require('es-abstract/2016/IsPropertyKey');
var SameValue = require('es-abstract/2016/SameValue');
var Type = require('es-abstract/2016/Type');

// https://www.ecma-international.org/ecma-262/6.0/#sec-createmethodproperty

module.exports = function CreateMethodProperty(O, P, V) {
	if (Type(O) !== 'Object') {
		throw new $TypeError('Assertion failed: Type(O) is not Object');
	}

	if (!IsPropertyKey(P)) {
		throw new $TypeError('Assertion failed: IsPropertyKey(P) is not true');
	}

	var newDesc = {
		'[[Configurable]]': true,
		'[[Enumerable]]': false,
		'[[Value]]': V,
		'[[Writable]]': true
	};
	return DefineOwnProperty(
		IsDataDescriptor,
		SameValue,
		FromPropertyDescriptor,
		O,
		P,
		newDesc
	);
};
