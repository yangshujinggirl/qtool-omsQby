'use strict';

var GetIntrinsic = require('es-abstract/GetIntrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var $hasInstance = GetIntrinsic('Symbol.hasInstance', true);

var Call = require('es-abstract/2018/Call');
var GetMethod = require('es-abstract/2018/GetMethod');
var IsCallable = require('es-abstract/2018/IsCallable');
var OrdinaryHasInstance = require('es-abstract/2018/OrdinaryHasInstance');
var ToBoolean = require('es-abstract/2018/ToBoolean');
var Type = require('es-abstract/2018/Type');

// https://www.ecma-international.org/ecma-262/6.0/#sec-instanceofoperator

module.exports = function InstanceofOperator(O, C) {
	if (Type(O) !== 'Object') {
		throw new $TypeError('Assertion failed: Type(O) is not Object');
	}
	var instOfHandler = $hasInstance ? GetMethod(C, $hasInstance) : void 0;
	if (typeof instOfHandler !== 'undefined') {
		return ToBoolean(Call(instOfHandler, C, [O]));
	}
	if (!IsCallable(C)) {
		throw new $TypeError('`C` is not Callable');
	}
	return OrdinaryHasInstance(C, O);
};
