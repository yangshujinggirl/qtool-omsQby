'use strict';

var GetIntrinsic = require('es-abstract/GetIntrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var $arraySlice = require('es-abstract/helpers/callBound')('Array.prototype.slice');

var Call = require('es-abstract/2017/Call');
var GetV = require('es-abstract/2017/GetV');
var IsPropertyKey = require('es-abstract/2017/IsPropertyKey');

// https://ecma-international.org/ecma-262/6.0/#sec-invoke

module.exports = function Invoke(O, P) {
	if (!IsPropertyKey(P)) {
		throw new $TypeError('P must be a Property Key');
	}
	var argumentsList = $arraySlice(arguments, 2);
	var func = GetV(O, P);
	return Call(func, O, argumentsList);
};
