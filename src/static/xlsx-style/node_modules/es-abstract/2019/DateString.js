'use strict';

var GetIntrinsic = require('es-abstract/GetIntrinsic');

var $TypeError = GetIntrinsic('%TypeError%');

var weekdays = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
var months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];

var $isNaN = require('es-abstract/helpers/isNaN');
var padTimeComponent = require('es-abstract/helpers/padTimeComponent');

var Type = require('es-abstract/2019/Type');
var WeekDay = require('es-abstract/2019/WeekDay');
var MonthFromTime = require('es-abstract/2019/MonthFromTime');
var YearFromTime = require('es-abstract/2019/YearFromTime');
var DateFromTime = require('es-abstract/2019/DateFromTime');

// https://www.ecma-international.org/ecma-262/9.0/#sec-datestring

module.exports = function DateString(tv) {
	if (Type(tv) !== 'Number' || $isNaN(tv)) {
		throw new $TypeError('Assertion failed: `tv` must be a non-NaN Number');
	}
	var weekday = weekdays[WeekDay(tv)];
	var month = months[MonthFromTime(tv)];
	var day = padTimeComponent(DateFromTime(tv));
	var year = padTimeComponent(YearFromTime(tv), 4);
	return weekday + '\x20' + month + '\x20' + day + '\x20' + year;
};
