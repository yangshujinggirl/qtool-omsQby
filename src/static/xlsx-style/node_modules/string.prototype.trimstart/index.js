'use strict';

var callBind = require('es-abstract/helpers/callBind');
var define = require('define-properties');

var implementation = require('string.prototype.trimstart/implementation');
var getPolyfill = require('string.prototype.trimstart/polyfill');
var shim = require('string.prototype.trimstart/shim');

var bound = callBind(getPolyfill());

define(bound, {
	getPolyfill: getPolyfill,
	implementation: implementation,
	shim: shim
});

module.exports = bound;
