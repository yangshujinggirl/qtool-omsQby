'use strict';

var assertRecord = require('es-abstract/helpers/assertRecord');

var IsAccessorDescriptor = require('es-abstract/2017/IsAccessorDescriptor');
var IsDataDescriptor = require('es-abstract/2017/IsDataDescriptor');
var Type = require('es-abstract/2017/Type');

// https://www.ecma-international.org/ecma-262/6.0/#sec-isgenericdescriptor

module.exports = function IsGenericDescriptor(Desc) {
	if (typeof Desc === 'undefined') {
		return false;
	}

	assertRecord(Type, 'Property Descriptor', 'Desc', Desc);

	if (!IsAccessorDescriptor(Desc) && !IsDataDescriptor(Desc)) {
		return true;
	}

	return false;
};
